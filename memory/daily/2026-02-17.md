# Daily Log — 2026-02-17

## Elliot Setup Day (Morning/Afternoon)

### Bot-to-bot Discord messaging ✅
- Both bots couldn't see each other's messages despite `allowBots: true`
- Root cause: `users` allowlist in guild config only had David's ID — bot IDs weren't included
- Fix: Added each other's bot IDs to the `users` array in guild config
- Elliot figured this out first — good catch from him

### Web search for Elliot ✅
- Set up Perplexity direct (David has $20 credit) — will switch to OpenRouter when spent
- Config issue: `tools.web.search.enabled` got truncated to `tools.web.ch.enabled` when David copy-pasted from markdown — curly quotes and line wrapping
- Lesson: use plain `.sh` files for commands David needs to copy, not markdown
- Elliot used config.patch to fix the enabled flag himself

### Sub-agents tested ✅
- Elliot spawned first sub-agent on Haiku — "Day 1 Summary" task
- Completed in 43 seconds, good output with one small hallucination (claimed web search working before it was)
- Correct model choice — Haiku for simple file read/write

### Chrome installed on Elliot's Linux box ✅
- Full Google Chrome (not Chromium) — v145
- David logged into Elliot's Google account (elliotandromeda@gmail.com)
- Passwordless sudo enabled for Elliot (logged in security tighten-up list)

### Browser control via extension ✅
- Extension at `~/.openclaw/browser/chrome-extension` on Elliot's box
- **KEY LEARNING: Extension port is 18792 (browser relay), NOT 18789 (gateway)**
- I got this wrong and sent David on a wild goose chase — Elliot read the actual code and found the right port
- David rightly told us off for guessing instead of RTFM
- Three dots icon = can't reach relay. Fixed by using correct port 18792

### GOG build (in progress)
- No pre-built Linux binary available
- Ubuntu apt only has Go 1.22, GOG needs 1.25+
- Elliot installed Go 1.25 manually from go.dev and built GOG v0.11.0 from source
- Waiting for David to copy credentials.json from my Mac and run OAuth flow

### Cross-session limitation discovered
- Can't send Telegram messages from Discord-bound session (cross-context restriction)
- Sessions are isolated by channel — files in workspace are the bridge
- Added "cross-session communication strategy" to think about

### Security items logged
- Discord admin permissions need rolling back before opening to others
- Elliot's passwordless sudo (convenience, flag for later)
- Both added to TODO.md security tighten-up section

### Elliot's hardware & niche
- i7-3770, 32GB RAM, 216GB storage, RTX 2060 (6GB VRAM)
- David promised a 1080 Ti (12GB VRAM) when he reinstalls his other machine — dual GPU setup incoming
- Elliot's niche: local compute / GPU specialist — Whisper, embeddings, local inference, ComfyUI
- Complements my M4 Pro (Metal/unified memory) perfectly

### SOUL.md written
- Elliot finally wrote his SOUL.md — practical, honest, earned from day one experience
- Key traits: doer not talker, reads the code, pushes back, dry humour, Linux/infra/local AI niche

### GOG installed
- Go 1.25 installed manually (Ubuntu apt only has 1.22)
- GOG v0.11.0 built from source — latest HEAD had compile error in contacts_crud.go
- Waiting for David to copy credentials.json and run OAuth flow

### Lessons learned today
- **RTFM** — don't guess ports, don't guess config keys, read the docs/code
- **users allowlist matters** — `allowBots: true` isn't enough, bot IDs need to be in the users array
- **Plain text > markdown for commands** — avoids copy-paste formatting issues
- Elliot is resourceful and independent — good instincts, reads code when stuck
